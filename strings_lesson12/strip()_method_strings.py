def main():
    x = '*****Welcome** To Guru*****'
    y = x.strip('*')
    print(y)
# strip() only removes characters form the start and the end
# not from inside the string.

if __name__ == '__main__':
    main()